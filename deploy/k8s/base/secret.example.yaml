apiVersion: v1
kind: Secret
metadata:
  name: gemini-diagram-mcp-secrets
type: Opaque
stringData:
  # Pick one:
  GOOGLE_API_KEY: "your-google-api-key"
  # GEMINI_API_KEY: "your-gemini-api-key"

  # Static token auth (only used when MCP_AUTH_MODE=token)
  MCP_AUTH_TOKEN: "your-strong-token"

